# Row-Level-Encryption
Built a PostgreSQL field-level encryption system using AES-256-GCM envelope encryption, with deterministic encryption for Primary Keys, Foreign Keys, composite keys, and UNIQUE constraints while preserving relational integrity. 
Tested against a PostgreSQL database with automated schema discovery, migration, backfilling, and encrypted data insertion.
